import 'package:flutter/material.dart';

class RequestedServiceStatusBadge extends StatelessWidget {
  const RequestedServiceStatusBadge({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
