import 'package:flutter/material.dart';

class RequestedServiceItemCard extends StatelessWidget {
  const RequestedServiceItemCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
