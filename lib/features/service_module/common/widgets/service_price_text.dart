import 'package:flutter/material.dart';

class ServicePriceText extends StatelessWidget {
  const ServicePriceText({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
