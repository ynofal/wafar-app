import 'package:flutter/material.dart';

class ServiceMediaHeader extends StatelessWidget {
  const ServiceMediaHeader({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
