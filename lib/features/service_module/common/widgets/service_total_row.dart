import 'package:flutter/material.dart';

class ServiceTotalRow extends StatelessWidget {
  const ServiceTotalRow({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
