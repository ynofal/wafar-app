import 'package:flutter/material.dart';

class BookingDetailsInfo extends StatelessWidget {
  const BookingDetailsInfo({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
