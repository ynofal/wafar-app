import 'package:flutter/material.dart';

class BookingDetailsStatus extends StatelessWidget {
  const BookingDetailsStatus({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
