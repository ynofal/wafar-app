import 'package:flutter/material.dart';

class ScheduledBookingBanner extends StatelessWidget {
  const ScheduledBookingBanner({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
