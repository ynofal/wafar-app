import 'package:flutter/material.dart';

class CustomServiceItemCard extends StatelessWidget {
  const CustomServiceItemCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
