import 'package:flutter/material.dart';

class ScheduleSectionWidget extends StatelessWidget {
  const ScheduleSectionWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
