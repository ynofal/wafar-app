import 'package:flutter/material.dart';

class CustomServiceSuccessSheet extends StatelessWidget {
  const CustomServiceSuccessSheet({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
