import 'package:flutter/material.dart';

class CustomServiceListScreen extends StatefulWidget {
  final int? createdRequestId;

  const CustomServiceListScreen({super.key, this.createdRequestId});

  @override
  State<CustomServiceListScreen> createState() => _CustomServiceListScreenState();
}

class _CustomServiceListScreenState extends State<CustomServiceListScreen> {
  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
