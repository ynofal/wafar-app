import 'package:flutter/material.dart';

class InstantServiceSection extends StatelessWidget {
  const InstantServiceSection({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox();
}
